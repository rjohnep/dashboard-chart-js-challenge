import { createAction } from 'redux-actions';

export const setActiveState = createAction('DASHBOARD: set_active_state');
export const resetActiveState = createAction('DASHBOARD: reset_active_state');
